---
title: Code post
publishedAt: '2021-09-05'
summary: 'Talvi tulee ja lunta sataa. Tässä on koodia ja tekstiä, molemmin päin. Myös värejä.'
tags: ['technical']
---

Ei tarvita erillistä otsikkoa, leipiksestä aloitus vaan. Ennen koodinpätkää vaakaviiva merkkaamaan eroa koodipätkän otsikkoon.

---

```jsx:kasper.works/snippet-directory/here
import Document, { Html, Head, NextScript, Main } from 'next/document';
import { ServerStyleSheet } from 'styled-components';

const Emphasis = styled.em`
  background-color: #235353;
  font-style: normal;
  padding: 0 2px;
`;

const Post = ({ code, frontmatter }) => {
  const Component = React.useMemo(() => getMDXComponent(code), [code]);
  return (
    <Container>
      <h1>{frontmatter.title}</h1>
      <Component components={{ a: CustomLink }} />
    </Container>
  );
};

```

Nostot tekstin sisällä **toimii täälläkin.**

Muuten olis ihan normitekstiä tässä, koodin selitystä ja semmoista. Katotaan mitä sitä keksii.

---

# Where to go from here:

- [Learning to Code at 35](/blog/new-coder-at-35)
